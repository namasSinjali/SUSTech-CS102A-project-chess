package com.backend.special_moves;

import com.ChessboardPoint;

public class PromotionPawnMove extends ChessboardPoint implements SpecialMove {
    public PromotionPawnMove(int x, int y) {
        super(x, y);
    }
}
