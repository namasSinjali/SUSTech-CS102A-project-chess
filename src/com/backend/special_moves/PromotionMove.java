package com.backend.special_moves;

import com.ChessboardPoint;

public class PromotionMove extends ChessboardPoint implements SpecialMove {
    public PromotionMove(int x, int y) {
        super(x, y);
    }
}
